pipeline {
  agent {
    node {
      label "$TEST_CLIENT"
    }
  }
  options {
        timestamps()
    }
  stages{
    stage('Shared Library') {
              steps {
               script {
	       echo "Hi"
              }
			  }
          }
     
       }

} 
