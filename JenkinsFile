pipeline {
  agent any
  options {
        timestamps()
    }
  stages{
    stage('Shared Library') {
              steps {
               script {
	       code = load "TestClass.groovy"
	       code.main()
	       echo "Hi"
              }
   }
          }
     
       }

} 
